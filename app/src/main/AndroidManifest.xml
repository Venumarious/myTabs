<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eduadministrative.mytabs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Notices">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TabFragment1" />
        <activity android:name=".TabFragment2" />
        <activity android:name=".TabFragment3" />

        <activity android:name="com.TabExample2.New_MainActivity" />
        <activity android:name="com.TabExample2.InboxActivity" />
        <activity android:name="com.TabExample2.OutboxActivity" />
        <activity android:name="com.TabExample2.ProfileActivity"></activity>
    </application>

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>